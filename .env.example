# Required for Claude triage / playbook generation
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override default model
CDP_MODEL=claude-sonnet-4-5

# Optional: where DuckDB files + Parquet logs live
CDP_DATA_DIR=./data
